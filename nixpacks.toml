[phases.setup]
nixPkgs = ["php82", "php82Packages.composer"]

[phases.install]
cmds = ["composer install --no-interaction --ignore-platform-req=ext-mongodb"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."